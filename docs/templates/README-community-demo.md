<!--
Template for a Community Demo — a focused, single-integration example, typically an external
contribution. Copy this into <your-demo-folder>/README.md and fill it in — delete these comments
and any section that genuinely doesn't apply. Keep it short: this template exists to make a
single integration story easy to read and run, not to document a whole system.

Look at dotnet-react-frontend/README.md or js-mcp-backend/README.md for a filled-in example.
-->
# <What calls what> (<Tech> → <Tech>)

**One or two bold sentences: the one thing this demo proves.**

> **Type:** Community Demo · **Owner:** @<your-github-handle>

<!-- Optional: keep this callout if your demo uses the shared AI-assistant rules, otherwise delete it.
> Graftcode's AI rules (`CLAUDE.md` + `.claude/rules/`) make the assistant build this the Graftcode
> way automatically. This repo doesn't vendor those rules; add them to your own project from
> [grft-dev/graftcode-demos/rules](../rules) (see the root [README](../README.md)).
-->

## Prerequisites
- <tool/runtime + version>

## Steps / Run it

```bash
# 1. ...
# 2. ...
```

## Why this matters
- <what this demo proves that a reader couldn't tell from a generic tutorial>

## Gotchas / Cleanup
- <anything a reader will trip over>
- <teardown command, if this spins up containers or cloud resources>
